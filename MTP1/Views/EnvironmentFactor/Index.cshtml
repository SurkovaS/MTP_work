@using MTP1
@using MTP1.Models
@model IEnumerable<EnvironmentFactor>
@{
    this.ViewBag.Title = string.Format("Факторы окружения | {0}", Constants.TITLE);
}

<link href="@this.Url.Content("~/Content/List.css")" rel="stylesheet" type="text/css" />
<div id="grid_container" style="width: 100%">
    <table id="grid" cellpadding="0" cellspacing="0">
    </table>
    <div id="pager" style="text-align: center;">
    </div>
    <div id="search" style="margin-left: 30%; display: none">
    </div>
</div>
<script type="text/javascript">
    var indexUrl = '@Url.Action("GetEnvFactors", "EnvironmentFactor")';
    var editUrl = '@Url.Action("Edit", "EnvironmentFactor")';
    var deleteUrl = '@Url.Action("Delete", "EnvironmentFactor")';
    var addUrl = '@Url.Action("Create", "EnvironmentFactor")';
    var viewUrl = '@Url.Action("Details", "EnvironmentFactor")';

    $(function () {

        DrawMTPGrid('grid', 'pager', indexUrl, editUrl, deleteUrl, addUrl, viewUrl,
            ['Сценарий тестирования', 'Фактор окружения', 'Весовой коэффициент', 'Сложность'],
            [
                {
                    name: 'UseCase',
                    sortable: false,
                    align: 'center',
                    width: '250'
                },
                {
                    name: 'EnvironmentFactor',
                    sortable: false,
                    align: 'center',
                    width: '100'
                },
                {
                    name: 'WeightCoefficient',
                    sortable: false,
                    align: 'left',
                    width: '50'
                },
                {
                    name: 'Difficulty',
                    sortable: false,
                    align: 'left',
                    width: '50'
                }
            ]);
    });

</script>
