@using MTP1
@model MTP1.Models.EnvironmentFactorDic

@{
    this.ViewBag.Title = string.Format("Элемент справочника | {0}", Constants.TITLE);
}

<p>
    @Html.ActionLink("Редактировать", "Edit", new { id = this.Model.ID }) |
    @Html.ActionLink("Типы факторов окружения", "Index")
</p>

<fieldset>
    <legend>Элемент справочника Факторы окружения</legend>
    <div class="display-label">Тип фактора окружения</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Title)
    </div>
    <div class="display-label">Описание</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Description)
    </div>
</fieldset>